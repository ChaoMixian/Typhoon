{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/backend", // 设置代码的路径
            "output": "${workspaceFolder}/backend/tests/Typhoon_test", // 指定编译文件路径
            "cwd": "${workspaceFolder}/backend/tests", // 指定运行时的工作目录
            "args": [], // 如果有额外的运行参数，可以在这里添加
            "env": {
                "DEBUG": "true"
            }
        }
    ]
}